<?xml version="1.0" encoding="utf-8"?>
<configuration>

  <!-- 
  This config file is empty and not used by the app. 
  Still it must be present so the build-job on azure publishes this project!
  -->
  
</configuration>